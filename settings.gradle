pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
    resolutionStrategy {
        eachPlugin {
            if (requested.id.id == "com.android.application") {
//                useModule("com.android.tools.build:gradle:${requested.version}")
                useModule("com.android.tools.build:gradle:gradle:4.2.1")
            }
        }
    }
}

rootProject.name = "TestApp"
include ':app'

